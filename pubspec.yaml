name: calendar_appbar
description: Flutter package for custom AppBar with full calendar view with many new features!
version: 0.0.6
repository: https://github.com/vpalcar/calendar_appbar

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
